---
# role: ansible-role-borg-backup
# file: vars/Alpine.yml

borg_backup_packages: []

borg_backup_package_available: True
borg_backup_package: borgbackup

borg_backup_binary_required_packages:
  - python3
  - py3-pyzmq
  - libzmq
  - libstdc++
  - libgcc
  - libsodium
  - libacl
  - lz4-libs
